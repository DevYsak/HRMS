{{-- HR Admin dashboard (/dashboard/hr-admin) — premium HR operations content.
     Shares the exact same self-contained widget set as the role-based "/" HR view
     via resources/views/dashboard/hr.blade.php (single source of truth, no duplication). --}}
@include('dashboard.hr')
